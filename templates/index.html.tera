<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
    </head>
    <body>
        <div class="container">
          <h1>WELCOME TO THE USER LIST APPLICATION</h1>
          {% for user in users %}
            <p>{{ user.name }} {{ user.key }}</p>
          {% endfor %}
          {% for priv in privs %}
            <p>{{ priv.user_id }} {{ priv.superuser }}</p>
          {% endfor %}
        </div>
    </body>
    <footer>
        <p style="text-align:center">Copyright 2019 Fan Jiang</p>
    </footer>
</html>